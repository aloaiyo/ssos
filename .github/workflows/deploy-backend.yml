name: Deploy Backend to EC2

on:
  push:
    branches: [main]
    paths:
      - 'backend/**'
      - '.github/workflows/deploy-backend.yml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to EC2 via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            set -e  # 에러 발생 시 즉시 중단
            export PATH="$HOME/.local/bin:$PATH"

            echo "========================================="
            echo "🚀 Starting deployment..."
            echo "========================================="

            cd ~/ssos/backend

            # 1. Pull latest code
            echo ""
            echo "📥 Pulling latest code..."
            git pull origin main

            # 2. Setup environment
            echo ""
            echo "⚙️ Setting up environment..."
            if [ -f .env.production ]; then
              cp .env.production .env
              echo "✅ Using .env.production"
            else
              echo "❌ .env.production not found!"
              echo "   Create .env.production with required settings:"
              echo "   - DATABASE_URL or USE_AWS_SSM=true"
              exit 1
            fi

            # 3. Validate required environment variables
            echo ""
            echo "🔍 Validating environment..."

            # Check if using SSM or local env
            if grep -q "USE_AWS_SSM=true" .env 2>/dev/null; then
              echo "   Mode: AWS SSM Parameter Store"
              # Verify SSM access
              if ! aws sts get-caller-identity --region ap-northeast-2 > /dev/null 2>&1; then
                echo "❌ EC2 cannot access AWS. IAM Role required!"
                echo "   Attach IAM Role with AmazonSSMReadOnlyAccess to EC2 instance"
                exit 1
              fi
              echo "✅ AWS access verified"
            else
              echo "   Mode: Local environment variables"
              # Verify DATABASE_URL exists
              if ! grep -q "^DATABASE_URL=" .env 2>/dev/null; then
                echo "❌ DATABASE_URL not found in .env!"
                echo "   Add DATABASE_URL to .env.production or enable SSM mode"
                exit 1
              fi
              echo "✅ DATABASE_URL found"
            fi

            # 4. Install dependencies
            echo ""
            echo "📦 Installing dependencies..."
            poetry install --only main --no-root

            # 5. Test app can start
            echo ""
            echo "🧪 Testing application..."
            if ! poetry run python -c "from app.main import app; print('✅ App imports OK')"; then
              echo "❌ Application failed to import!"
              echo "   Check for syntax errors or missing dependencies"
              exit 1
            fi

            # 6. Run migrations
            echo ""
            echo "🗄️ Running database migrations..."

            # Check if aerich table exists (indicates DB is initialized)
            AERICH_EXISTS=$(poetry run python -c "
            import asyncio
            from tortoise import Tortoise
            from app.config import settings, TORTOISE_ORM

            async def check():
                await Tortoise.init(config=TORTOISE_ORM)
                conn = Tortoise.get_connection('default')
                try:
                    await conn.execute_query('SELECT 1 FROM aerich LIMIT 1')
                    print('exists')
                except:
                    print('not_exists')
                await Tortoise.close_connections()

            asyncio.run(check())
            " 2>/dev/null || echo "error")

            if [ "$AERICH_EXISTS" = "not_exists" ]; then
              echo "   Database not initialized. Running aerich init-db..."
              if poetry run aerich init-db 2>&1; then
                echo "✅ Database initialized successfully"
              else
                echo "❌ Database initialization failed!"
                exit 1
              fi
            elif [ "$AERICH_EXISTS" = "exists" ]; then
              echo "   Database already initialized. Running aerich upgrade..."
              MIGRATION_OUTPUT=$(poetry run aerich upgrade 2>&1) || true
              echo "$MIGRATION_OUTPUT"
              if echo "$MIGRATION_OUTPUT" | grep -q "error"; then
                echo "❌ Migration failed!"
                exit 1
              fi
              echo "✅ Migrations applied"
            else
              echo "❌ Cannot connect to database!"
              exit 1
            fi

            # Verify tables exist
            TABLE_COUNT=$(poetry run python -c "
            import asyncio
            from tortoise import Tortoise
            from app.config import TORTOISE_ORM

            async def count():
                await Tortoise.init(config=TORTOISE_ORM)
                conn = Tortoise.get_connection('default')
                result = await conn.execute_query(\"SELECT count(*) FROM information_schema.tables WHERE table_schema = 'public'\")
                print(result[1][0]['count'])
                await Tortoise.close_connections()

            asyncio.run(count())
            " 2>/dev/null || echo "0")

            echo "   Tables in database: $TABLE_COUNT"
            if [ "$TABLE_COUNT" -lt 5 ]; then
              echo "⚠️ Warning: Expected more tables. DB may not be properly initialized."
            else
              echo "✅ Database schema verified"
            fi

            # 7. Setup systemd service
            echo ""
            echo "🔧 Configuring systemd service..."
            printf '%s\n' \
              '[Unit]' \
              'Description=SSOS Backend API' \
              'After=network.target' \
              '' \
              '[Service]' \
              'Type=simple' \
              'User=ubuntu' \
              'WorkingDirectory=/home/ubuntu/ssos/backend' \
              'Environment="PATH=/home/ubuntu/.local/bin:/usr/local/bin:/usr/bin:/bin"' \
              'EnvironmentFile=/home/ubuntu/ssos/backend/.env' \
              'ExecStart=/home/ubuntu/.local/bin/poetry run uvicorn app.main:app --host 0.0.0.0 --port 8000' \
              'Restart=always' \
              'RestartSec=3' \
              'StandardOutput=journal' \
              'StandardError=journal' \
              '' \
              '[Install]' \
              'WantedBy=multi-user.target' | sudo tee /etc/systemd/system/ssos-backend.service > /dev/null
            sudo systemctl daemon-reload
            sudo systemctl enable ssos-backend
            echo "✅ systemd service configured"

            # 8. Restart service
            echo ""
            echo "🔄 Restarting service..."
            sudo systemctl restart ssos-backend

            # 9. Wait and verify
            echo ""
            echo "⏳ Waiting for service to start..."
            sleep 5

            # Check service status
            if sudo systemctl is-active --quiet ssos-backend; then
              echo "✅ Service is running"

              # Health check
              for i in 1 2 3; do
                if curl -sf http://localhost:8000/docs > /dev/null 2>&1; then
                  echo ""
                  echo "========================================="
                  echo "✅ Deployment completed successfully!"
                  echo "========================================="
                  exit 0
                fi
                echo "   Attempt $i/3: Waiting for API..."
                sleep 2
              done

              echo "⚠️ Service running but API not responding"
            else
              echo "❌ Service failed to start"
            fi

            # Debug info on failure
            echo ""
            echo "========================================="
            echo "❌ Deployment failed. Debug info:"
            echo "========================================="
            echo ""
            echo "--- Service Status ---"
            sudo systemctl status ssos-backend --no-pager || true
            echo ""
            echo "--- Recent Logs ---"
            sudo journalctl -u ssos-backend -n 50 --no-pager || true
            exit 1
